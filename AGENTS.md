# 个人网站项目协作说明

## 项目概述

这是王冬青的个人求职作品集，用于展示 B 端产品能力、AI 产品实践和 Vibe Coding 交付能力。

网站的核心任务是让招聘者和产品负责人快速形成明确认知：

> 懂 B 端复杂业务，能从 0 到 1 搭建后台产品，并能借助 AI 与 Vibe Coding 加快验证和交付。

目标访客：

- 招聘者与用人经理
- 产品负责人
- 技术开发者
- 同行产品经理

## 当前交付形态

- 主文件：`index.html`
- 架构：单页、单文件、纯 HTML/CSS/JavaScript
- 所有样式、脚本和项目图片均内嵌在 `index.html`
- 不使用框架、包管理器、构建工具、在线字体或外部 UI 依赖
- 文件应支持直接双击打开，也应支持任意静态服务器托管
- 当前不绑定具体部署平台；用户后续提供部署方式和地址

除非用户明确改变要求，后续迭代必须维持单文件交付，不拆分 CSS、JavaScript 或图片资源。

## 个人定位

- 约 5 年互联网产品经验，主要专注 B 端产品。
- 能独立完成后台系统的需求分析、产品架构、流程设计、交互原型、PRD、测试和上线。
- 具备 AI 产品实践，包括文档校验、智能分类、关键信息提取、Prompt 优化和 Agent 工作流。
- 能使用 Vibe Coding 构建 HTML 交互原型，辅助需求验证和产研沟通。
- 具备数学与信息通信工程背景，熟悉 SQL、Python，能够与技术团队有效协作。

所有能力描述必须由简历、项目文档或用户确认的信息支持，不得扩张为没有证据的能力标签。

## 页面信息架构

当前 `index.html` 包含以下区域，后续应保持整体叙事顺序：

1. 首屏：姓名、职业定位、价值主张、项目与联系入口、粒子网络。
2. 核心能力：B 端后台、AI、全流程交付、Vibe Coding、数据与技术协作。
3. 核心项目：非结构化文档归档中台、档案智能化（AI 分类、提取与校验）两个企业案例，以及“AI 客服系统”开发中占位案例。
4. 工作经历：三段经历及可验证成果。
5. Vibe Coding：需求理解、PRD、交互原型、验证迭代的方法说明。
6. 联系区域：姓名和邮箱。

网站不是简历全文搬运。首页信息必须适合快速浏览，复杂规则放入可展开案例中。

## 视觉与交互规范

视觉方向：浅色“产品工作手记 / 案例档案册”风格，专业、克制、清晰且具有编辑感。

现有视觉系统：

- 主背景：冷白灰绿色纸张底色，内容卡片使用更明亮的纸白色
- 主文字：深墨绿色；结构强调使用墨绿，朱红仅用于批注、问题、判断和开发中状态
- 组件语言：产品工作手记、案例索引、批注轨道、规则节点、档案表格与细线网格
- 字体：系统字体栈，保证离线可用；标题优先使用系统宋体，正文使用系统无衬线字体，数据与标签使用等宽字体
- 标志性效果：首屏浅色 Canvas 业务节点网络与贯穿页面的案例批注感

设计约束：

- 编辑感服务于“真实产品判断与案例证据”，不能做成泛化的复古简历或报纸模板。
- 粒子动画是唯一主要环境特效，使用低对比墨绿连线和少量朱红节点，其他动效保持克制。
- 首屏必须优先传达姓名、定位和价值，不让特效遮挡内容。
- 保持清晰的信息层级、充分留白和高文字对比度。
- 不使用无意义的装饰、虚假数据面板或与项目无关的生成式科技图片。
- 新增设计或重构页面时优先使用 `frontend-design` Skill。

现有交互包括：

- 锚点导航
- 粒子邻近连线与鼠标交互
- 滚动显现
- 项目案例展开与收起
- 项目截图灯箱放大
- 邮箱复制

必须保留 `prefers-reduced-motion` 降级，并控制移动端粒子数量和动画负载。

## 内容事实与来源

### 个人经历

主要来源：`王同学-产品经理.pdf`

允许展示的身份信息：

- 姓名：王冬青
- 邮箱：`qqing2021@163.com`
- 城市：北京

手机号不公开，原始简历不提供下载入口。

### 非结构化文档归档中台

主要来源：

- 最新个人简历：`王同学-产品经理.pdf`
- 用户指定的飞书知识库“工作项目”：<https://my.feishu.cn/wiki/PYp0wpSz5ipqD2kCG52csdixnme?fromScene=spaceOverview>（内部补充来源，不得在公开网站暴露该链接或其子页面地址）
- 公开原型：<https://xdqqing.github.io/Work-projects/>
- 公开原型中的《产品说明》《系统需求文档》《AI校验需求文档》

飞书外层文档名称为《V1.0 非结构化文档归档中台-产品需求说明书（PRD）》，文档内产品名称为“非结构化文档归档统一服务-产品需求说明书”。网站案例主标题使用“非结构化文档归档中台”，并在摘要中说明正式产品名称“非结构化文档归档统一服务”；公开原型产品界面可使用“智能文档归档平台”。不要再把“业档一体”放在案例主标题中。

案例重点：

- 从 0 到 1 建设企业非结构化文档统一归档后台，覆盖需求分析、产品设计、交互原型、测试与上线。
- 上传申请、审批、电子文件台账、档案分类、档案目录、权限与消息通知。
- 7 类审批状态与 4 类归档状态双轨建模，包含撤回、驳回、异常处理和审批证据链。
- 数据权限按照管理组织范围与档案分类范围取交集，并依赖人员、管理组织、法人和项目主数据。
- 审批通过后按“一份文件生成一条电子文件台账”沉淀结果；系统保存原始格式和长期保存格式。
- 使用 Vibe Coding 构建 HTML 原型并验证产品方案。

### 档案智能化：AI 分类、提取与校验

主要来源：公开原型中的《AI校验需求文档》、飞书知识库中的《AI分类与提取需求文档》《AI校验功能-需求文档说明》以及最新个人简历。

AI 分类与提取重点：

- 上传文件后自动匹配档案目录中的资料类型，并允许提交前人工修正。
- 根据资料类型自动提取题名、责任者、成文日期、文号等关键元数据。
- 分类规则库和提取规则库支持维护匹配依据、提取位置、规则文本与启停状态。
- 通过置信度、来源标记、低置信度处理、人工修正、重新提取、排队任务和异常状态形成人机协作闭环。
- LangGraph 工作流包含数据预获取、分类加提取与仅提取等交互路径；页面对外可概括为 Agent 工作流，不需要披露内部接口细节。

AI 校验定位：归档前的智能质量门禁。

案例重点：

- AI 校验作为审批首节点。
- 通用规则与指定资料类型专项规则匹配。
- 规则可配置、校验可解释、结果可追溯、流程可衔接、整改可闭环。
- 6 大检查维度、12 个标准检查项。
- 不通过项展示校验定位、判定依据、风险等级和修改建议。
- 支持替换文件后重新校验，或填写说明后进入人工复核。
- 当前版本的强控制配置仅用于展示，不做系统拦截；不得写成已经实施强拦截。
- Agent 连续三次调用失败或超时后显示“AI 校验异常”，系统自动进入人工复核节点，保障主流程可恢复。

当前网站内嵌两张经过筛选和压缩的真实界面截图。替换或新增截图前必须检查人员姓名、文件名、业务数据和其他内部信息，并完成脱敏。

### AI 客服系统个人项目

- 项目状态：正在开发中，尚未形成已交付成果。
- 目标：由 1 人打造真实线上可用的智能客服系统。
- 计划范围：前端交互、后端代码、权限管理、数据存储、Agent 搭建、云服务器部署等全链路实现。
- 对外表达必须明确“正在开发中”，不得把计划范围写成已经完成的能力或成果。
- 达到可演示状态后，再补充真实架构、关键取舍、线上地址和复盘。

### 其他可展示成果

- 机票二次行李自动出票，释放客服团队约 50% 人力。
- 航司退税流程线上化，串联业务、客服和财务角色。
- 财务自动对账，将手工操作优化为一键式处理。
- 企业客户使用行为分析，支持百万级年费客户续约。

不得虚构 AI 准确率、效率提升比例、用户数量或其他材料中没有明确记录的数据。

## 内容表达原则

- 使用真实、可验证的信息，不虚构项目成果、职责或技术能力。
- 案例优先采用“问题 - 判断 - 方案 - 结果 - 复盘”的结构。
- 重点说明本人职责和关键产品判断，不将团队成果全部包装为个人成果。
- 不只展示界面，要说明流程、规则、异常和业务价值。
- 将 Vibe Coding 定位为产品验证与交付加速方法，而不是单纯展示代码。
- 文案保持专业、简洁、具体，避免“赋能、引领、颠覆”等空泛表达。
- 当前默认中文，不自行增加英文版。

## 隐私与安全边界

- 不公开手机号、住址、身份证明或其他未确认的个人信息。
- 不公开公司内部数据、客户信息、账号、密钥、内网地址和未公开规则。
- 不提供原始简历、PRD、需求文档和操作手册的下载入口。
- 企业项目内容和截图必须脱敏。
- 外部链接使用 `target="_blank"` 时必须同时使用 `rel="noopener noreferrer"`。
- 不向第三方服务上传项目材料，除非用户明确授权具体材料和目标服务。

## 代码修改规范

- 保持语义化 HTML、清晰的 CSS 分区和原生 JavaScript。
- 不为了展示技术复杂度引入框架、依赖或构建流程。
- 新增图片时优先压缩为 WebP/JPEG Data URL 内嵌，并控制文件大小。
- 保持桌面端、平板端和手机端响应式布局。
- 所有功能按钮应可通过键盘操作并提供可见焦点状态。
- 图片必须提供准确的 `alt` 文本，Canvas 装饰应对辅助技术隐藏。
- 不删除减少动态效果、跳到主要内容等无障碍支持。
- 修改文案时同步检查页面标题和 `meta description` 是否仍准确。
- 修改邮箱或原型地址时，检查页面中所有重复入口，保持一致。

## 验收要求

每次重要修改后至少检查：

1. `index.html` 可直接打开且可通过本地静态服务器访问。
2. JavaScript 语法正确，控制台无页面脚本错误。
3. 桌面端和手机端无横向溢出、遮挡、文字裁切或不可点击区域。
4. 粒子动画、项目展开、截图灯箱、锚点和邮箱复制正常。
5. 内嵌图片能够加载，且没有敏感信息。
6. 邮箱与项目原型链接正确，外链安全属性完整。
7. 页面不依赖在线字体、CDN、框架或额外本地文件。
8. `prefers-reduced-motion` 下页面仍完整可用。
9. 手机号和原始文档下载入口未被意外加入。

前端视觉检查优先使用 Browser Skill；如果运行环境无法完成浏览器截图，必须执行静态结构、脚本语法、链接、图片与隐私检查，并在交付说明中披露未完成的视觉验收。

## Agent 权限与发布边界

Agent 可以自主：

- 阅读和分析当前项目材料。
- 修改网站代码、文案和内嵌资源。
- 安装完成开发与验收所需的合理 Skill 或临时工具。
- 运行本地服务、测试和质量检查。

GitHub 发布约定：

- 本项目提交和推送直接使用系统 Git，不调用 GitHub 发布 Skill，也不要求 GitHub CLI。
- 默认远程仓库：`git@github.com:xdqqing/personal-website.git`
- 默认分支：`main`
- 当前项目固定使用 SSH 密钥：`/Users/wdq/.ssh/id_ed25519_github_pm`
- 当前仓库的 `core.sshCommand` 应保持为：`ssh -i /Users/wdq/.ssh/id_ed25519_github_pm -o IdentitiesOnly=yes`
- 每次提交和推送默认只包含 `index.html` 与 `AGENTS.md`。
- PDF、DOCX、PPTX、视频及其他项目素材保持本地，不加入 Git 跟踪。
- 不使用 GitHub HTTPS 远程进行推送；当前环境访问 `github.com:443` 会超时，而 SSH 通道已经验证可用。

用户明确要求提交或发布时，按以下顺序执行：

1. 运行 `git status --short --branch`，确认当前分支和工作区变化。
2. 检查远程地址与 `core.sshCommand`，确保使用上述 SSH 仓库和专用密钥。
3. 使用 `git add -- index.html AGENTS.md`，不得使用 `git add .` 或 `git add -A`。
4. 使用 `git diff --cached --name-only` 确认暂存区只有 `index.html` 和 `AGENTS.md`。
5. 创建普通提交，不修改或合并已有提交，除非用户明确要求。
6. 使用 `git push -u origin main` 完成首次上游设置；后续使用 `git push`。
7. 推送后核对 `main` 正在跟踪 `origin/main`，并确认其他素材仍为未跟踪状态。

首次推送本身不会导致连接失败。若推送异常，先检查远程是否误设为 HTTPS；不要连续重复 HTTPS 推送，也不要为此安装 GitHub CLI。

未经用户明确确认，Agent 不得：

- 推送远程 Git 仓库。
- 执行正式部署、绑定域名或修改线上环境。
- 公开新的个人信息或企业内部材料。
- 将单文件网站重构为框架项目或多文件工程。

用户负责最终内容确认，并在后续提供部署方式和发布地址。Agent 可以根据用户提供的平台准备适配和发布操作。
